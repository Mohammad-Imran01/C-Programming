#include<iostream>
using namespace std;

int divider(int A[], int l, int h)
{
    if(l<h){
        int m = (l+h)/2;
        divider(A,l,m);
        divider(A,m+1,h);
        merger(A,)
    }
}
int merger()
{
    if()
}
   

int main()
{
    int A[] = {1,2,3,4,5};
    Merge(A,0,4);
}